services:
  - type: web
    name: bpo-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port=10000
    envVars:
      - key: PORT
        value: 10000
